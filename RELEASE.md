# v2.0.0 - Steam 后台与 FH6Auto 联动版

## 新增

- `PrintWindow + PostMessage` 后台截图与后台输入，不移动物理鼠标。
- 以 `1600×900` 及以上作为重点适配尺寸；更小窗口仍可启动并按比例尝试。
- 独立抽奖、FH6Auto 联动两种模式。
- FH6Auto 风格深色界面，以及可记忆选择的中文 / English 切换。
- 普通/超级抽奖指定次数或抽到耗尽。
- 重复车“按价格、全部出售、全部保留”三种策略。
- 普通、超级、重复车、保留、出售、重复车出售收入 CR、OCR 失败、联动轮次累计统计；抽奖奖励 CR 不计入收入。
- FH6Auto 中间轮暂停/恢复握手，以及最终诊断报告落盘后开奖。
- 更新不兼容时不接管的安全协议，以及异常退出后的配置恢复备份。

## 修复

- EasyOCR 模型缺失时允许首次运行自动下载，并在首辆需要价格的重复车出现时等待初始化结果。
- OCR 失败不再把未知价格车辆当作低价车出售。
- 重复车关键按键改用同步后台消息，确认弹窗消失后才提交车辆与收入统计。
- 未知页面不再无限按 Enter；增加绝对超时和推进上限。
- 联动模式隔离 F9，F8 可同时紧急停止官方流程和开奖器。
- 暂停日志后等待官方输入层释放残余按键；联动失败会以 F8 停止官方流程。

## 更新边界

官方 `FH6Auto.exe` 未修改。后续仍可直接下载并覆盖官方 EXE；FH6Gacha 作为独立程序保留。

## 发布前验证

纯逻辑测试已包含在 `build.bat`。Windows FH6 Steam 实机项目见 README 的“Windows 实机验收清单”，完成该清单前不应宣称模板识别和联动时序已经实机验证。
